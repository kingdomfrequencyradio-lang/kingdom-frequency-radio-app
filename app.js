const radio=document.getElementById('radio'),playButton=document.getElementById('playButton'),playIcon=document.getElementById('playIcon'),playText=document.getElementById('playText'),statusText=document.getElementById('statusText'),volume=document.getElementById('volume'),shareButton=document.getElementById('shareButton'),installButton=document.getElementById('installButton'),installHelp=document.getElementById('installHelp');let deferredPrompt=null;radio.volume=Number(volume.value);function setPlayerState(p){playIcon.textContent=p?'❚❚':'▶';playText.textContent=p?'Pause Kingdom Frequency Radio™':'Play Kingdom Frequency Radio™';statusText.textContent=p?'Live now':'Ready to listen'}playButton.addEventListener('click',async()=>{if(radio.paused){statusText.textContent='Connecting…';try{await radio.play();setPlayerState(true)}catch(e){statusText.textContent='Unable to connect. Tap again.'}}else{radio.pause();setPlayerState(false)}});radio.addEventListener('playing',()=>setPlayerState(true));radio.addEventListener('pause',()=>setPlayerState(false));radio.addEventListener('waiting',()=>statusText.textContent='Buffering…');radio.addEventListener('error',()=>statusText.textContent='Stream unavailable. Please try again.');volume.addEventListener('input',()=>radio.volume=Number(volume.value));shareButton.addEventListener('click',async()=>{const d={title:'Kingdom Frequency Radio™',text:'Listen live to Kingdom Frequency Radio™ — Tuned to Truth. Powered by the Word.',url:location.href};try{navigator.share?await navigator.share(d):(await navigator.clipboard.writeText(location.href),statusText.textContent='App link copied')}catch(e){}});window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;installButton.hidden=false});installButton.addEventListener('click',async()=>{if(!deferredPrompt)return;deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;installButton.hidden=true});const isIos=/iphone|ipad|ipod/i.test(navigator.userAgent),isStandalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone;if(isIos&&!isStandalone)installHelp.innerHTML='On iPhone, tap the <strong>Share</strong> button in Safari, then choose <strong>Add to Home Screen</strong>.';if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('service-worker.js'));